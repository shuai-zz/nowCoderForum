<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.interaction.infrastructure.mapper.CommentMapper">

    <select id="selectTopRepliesGrouped" resultType="com.example.interaction.domain.entity.Comment">
        select *
        from (select c.*, ROW_NUMBER() over (
            PARTITION BY entity_id order by create_time desc
            ) as rn
              from comment c
              where c.entity_type=2
              and c.status=0
              and c.entity_id in
              <foreach collection="parentIds" item="pid" open="(" separator="," close=")">
                  #{pid}
              </foreach>

             ) t
        where t.rn &lt;=#{limit}
    </select>
</mapper>